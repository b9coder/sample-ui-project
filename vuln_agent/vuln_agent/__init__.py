"""Vulnerability chat agent with a display-element presentation layer.

Turns MCP tool results + LLM answers into a JSON payload of renderable
display rows (markdown, charts, download rows, filter panels, tables,
stat cards) that the React UI renders 1:1.
"""
